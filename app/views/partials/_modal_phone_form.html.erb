<div id="modal-number-container">
  <%= f.fields_for(:phones) do |p| %>
    <%= render partial: 'partials/phone_fields', locals: {f: p} %>
  <% end %>
</div>
<div class="col-xs-12"><%= link_to_add_association 'Add Phone', f, :phones, class: 'btn btn-primary', partial: 'partials/phone_fields' %></div>
