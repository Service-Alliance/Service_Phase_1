<%= form_for(@upload_category) do |f| %>
  <% if @upload_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@upload_category.errors.count, "error") %> prohibited this upload_category from being saved:</h2>

      <ul>
      <% @upload_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
