<%= form_for(@notify_type) do |f| %>
  <% if @notify_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notify_type.errors.count, "error") %> prohibited this notify_type from being saved:</h2>

      <ul>
      <% @notify_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
