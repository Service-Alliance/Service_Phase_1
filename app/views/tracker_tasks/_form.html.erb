<%= form_for(@tracker_task) do |f| %>
  <% if @tracker_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tracker_task.errors.count, "error") %> prohibited this tracker_task from being saved:</h2>

      <ul>
      <% @tracker_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
