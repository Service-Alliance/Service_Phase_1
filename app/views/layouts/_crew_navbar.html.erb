<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">

      <button type="button" class="navbar-toggle collapsed pull-left"  href="#menu-toggle" id="menu-toggle">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Service Alliance Inc.</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">
        <li>
          <% if current_user.notifications.where(viewed: false).count > 0 %>
            <%= link_to notifications_path  do %>
            Notifications <span class="badge-red"><%= current_user.notifications.where(viewed: false).count %></span>
            <% end %>
          <% else %>
            <%= link_to notifications_path  do %>
            Notifications <span class="badge"><%= current_user.notifications.where(viewed: false).count %></span>
            <% end %>
          <% end %>

        </li>
        <li><%= link_to "Wiki", articles_path %></li>
        <li><%= link_to "My Jobs", user_job_assignments_path(current_user) %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Contacts <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Franchises", franchises_path %></li>
            <li><%= link_to "Users", users_path %></li>
          </ul>
        </li>

        <li><%= link_to "Log Book", jobs_path %></li>
        <li> <%= link_to "Work Orders", work_orders_path%> </li>

        <% if user_signed_in? %>
          <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
        <% else %>
          <li><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
