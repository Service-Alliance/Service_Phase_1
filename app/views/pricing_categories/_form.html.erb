<%= form_for(@pricing_category) do |f| %>
  <% if @pricing_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pricing_category.errors.count, "error") %> prohibited this pricing_category from being saved:</h2>

      <ul>
      <% @pricing_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
