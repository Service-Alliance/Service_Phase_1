<%= form_for(@structure_type) do |f| %>
  <% if @structure_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@structure_type.errors.count, "error") %> prohibited this structure_type from being saved:</h2>

      <ul>
      <% @structure_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
