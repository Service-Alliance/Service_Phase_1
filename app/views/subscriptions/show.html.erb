<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @subscription.user_id %>
</p>

<p>
  <strong>Job:</strong>
  <%= @subscription.job_id %>
</p>

<%= link_to 'Edit', edit_job_subscription_path(@job, @subscription) %> |
<%= link_to 'Back', job_subscriptions_path(@job) %>
