<%= form_for(@franchise_user) do |f| %>
  <% if @franchise_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@franchise_user.errors.count, "error") %> prohibited this franchise_user from being saved:</h2>

      <ul>
      <% @franchise_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :franchise_id %><br>
    <%= f.number_field :franchise_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
