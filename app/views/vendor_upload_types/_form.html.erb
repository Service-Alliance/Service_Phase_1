<%= form_for(@vendor_upload_type) do |f| %>
  <% if @vendor_upload_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vendor_upload_type.errors.count, "error") %> prohibited this vendor_upload_type from being saved:</h2>

      <ul>
      <% @vendor_upload_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
