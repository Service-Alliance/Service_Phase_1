<%= form_for(@customer_vendor) do |f| %>
  <% if @customer_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_vendor.errors.count, "error") %> prohibited this customer_vendor from being saved:</h2>

      <ul>
      <% @customer_vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.number_field :customer_id %>
  </div>
  <div class="field">
    <%= f.label :vendor_id %><br>
    <%= f.number_field :vendor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
